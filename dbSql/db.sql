
use CustomersDB
select * from products

/* CREATE TABLE products (
    id INT IDENTITY (1, 1) primary key,
    name VARCHAR(100) NOT NULL,
    price DECIMAL(10, 2),
    quantity INT ,
    description Text
);  
 */

 --select * from products